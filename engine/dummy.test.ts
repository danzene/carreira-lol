import { describe, expect, it } from "vitest";

// Teste dummy da Fase 0: confirma que o Vitest roda. Some quando o motor chegar.
describe("setup (Fase 0)", () => {
  it("o ambiente de testes funciona", () => {
    expect(1 + 1).toBe(2);
  });
});

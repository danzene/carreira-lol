import { describe, it, expect } from "vitest";

// Teste dummy da Fase 0: só confirma que o Vitest está configurado e rodando.
// Será substituído pelos testes do motor na Fase 2.
describe("setup do projeto (Fase 0)", () => {
  it("o ambiente de testes funciona", () => {
    expect(1 + 1).toBe(2);
  });
});
